services:
  db:
    image: postgres:16
    container_name: nanchesoft-db
    restart: always
    environment:
      POSTGRES_DB: nanchesoftdb1
      POSTGRES_USER: nancheadmin
      POSTGRES_PASSWORD: CambiaEstaClave123*
    volumes:
      - postgres_data:/var/lib/postgresql/data
    ports:
      - "5432:5432"

volumes:
  postgres_data:
