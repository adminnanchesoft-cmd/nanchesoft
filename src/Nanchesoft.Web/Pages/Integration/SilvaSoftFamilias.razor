@page "/integration/silvasoft/familias"
@inject NavigationManager Nav

@code {
    protected override void OnInitialized()
        => Nav.NavigateTo("/integraciones/silvasoft/familias-materiales", replace: true);
}
