@page "/human-resources/empleado"
@page "/human-resources/empleado/{EmployeeId:guid}"
@using System.Net.Http.Json
@inject IHttpClientFactory HttpClientFactory
@inject NavigationManager Nav

<PageTitle>@(_isNew ? "Nuevo colaborador" : "Editar colaborador") | Nanchesoft</PageTitle>

<style>
    .ns-section { background:#fff; border-radius:8px; border:1px solid #e2e8f0; box-shadow:0 1px 3px rgba(0,0,0,.06); overflow:hidden; margin-bottom:16px; }
    .ns-section-header { display:flex; align-items:center; gap:8px; padding:10px 16px; border-bottom:1px solid #e2e8f0; font-weight:600; font-size:13px; letter-spacing:.3px; }
    .ns-section-body { padding:16px; }
    .ns-field-label { font-size:12px; font-weight:600; color:#475569; text-transform:uppercase; letter-spacing:.4px; margin-bottom:4px; display:block; }
    .ns-form-input { border-color:#d1d5db; border-radius:6px; font-size:14px; }
    .ns-form-input:focus { border-color:#1D4ED8; box-shadow:0 0 0 3px rgba(29,78,216,.1); }
    .ns-tab-nav .nav-link { border:none; border-radius:8px; padding:8px 16px; font-size:13px; font-weight:500; color:#64748b; display:flex; align-items:center; gap:6px; transition:all .15s; }
    .ns-tab-nav .nav-link.active { background:#1D4ED8; color:#fff; }
    .ns-tab-nav .nav-link:not(.active):hover { background:#f1f5f9; color:#1e40af; }
    .ns-header { background:#fff; border-bottom:1px solid #e2e8f0; padding:12px 20px; position:sticky; top:0; z-index:100; display:flex; align-items:center; gap:12px; box-shadow:0 1px 4px rgba(0,0,0,.06); }
    .ns-badge-active { background:#dcfce7; color:#166534; border-radius:20px; font-size:11px; padding:2px 10px; font-weight:600; }
    .ns-badge-inactive { background:#fee2e2; color:#991b1b; border-radius:20px; font-size:11px; padding:2px 10px; font-weight:600; }
    .ns-plus-btn { width:34px; height:34px; border-radius:6px; border:1px solid #d1d5db; background:#f8fafc; display:inline-flex; align-items:center; justify-content:center; cursor:pointer; font-size:18px; font-weight:700; color:#475569; flex-shrink:0; transition:all .15s; line-height:1; }
    .ns-plus-btn:hover { background:#1D4ED8; color:#fff; border-color:#1D4ED8; }
    .ns-fk-group { display:flex; gap:6px; align-items:stretch; }
    .ns-fk-group select, .ns-fk-group .form-select { flex:1; min-width:0; }
    /* Color variants */
    .ns-blue .ns-section-header { background:#EFF6FF; border-left:4px solid #1D4ED8; color:#1e40af; }
    .ns-green .ns-section-header { background:#F0FDF4; border-left:4px solid #15803D; color:#166534; }
    .ns-orange .ns-section-header { background:#FFF7ED; border-left:4px solid #C2410C; color:#9a3412; }
    .ns-purple .ns-section-header { background:#F5F3FF; border-left:4px solid #6D28D9; color:#5b21b6; }
    .ns-amber .ns-section-header { background:#FFFBEB; border-left:4px solid #D97706; color:#92400e; }
    .ns-cyan .ns-section-header { background:#F0F9FF; border-left:4px solid #0369A1; color:#075985; }
    .ns-slate .ns-section-header { background:#F8FAFC; border-left:4px solid #475569; color:#334155; }
    .ns-rose .ns-section-header { background:#FFF1F2; border-left:4px solid #BE123C; color:#9f1239; }
    .ns-tab-nav { gap:4px; flex-wrap:wrap; }
    .ns-breadcrumb { font-size:12px; color:#94a3b8; }
    .ns-breadcrumb a { color:#94a3b8; text-decoration:none; }
    .ns-breadcrumb a:hover { color:#1D4ED8; }
    .ns-breadcrumb .sep { margin:0 4px; }
</style>

<!-- ══════════ HEADER STICKY ══════════ -->
<div class="ns-header">
    <button class="btn btn-link p-0 text-secondary" @onclick='() => Nav.NavigateTo("/human-resources/employees")' title="Volver">
        <span class="bi bi-arrow-left fs-5"></span>
    </button>
    <div class="flex-grow-1">
        <div class="ns-breadcrumb mb-0">
            <a href="/human-resources/employees">RRHH</a>
            <span class="sep">/</span>
            <a href="/human-resources/employees">Colaboradores</a>
            <span class="sep">/</span>
            <span class="text-dark fw-semibold">@(_isNew ? "Nuevo" : _form.FullName())</span>
        </div>
        <div class="d-flex align-items-center gap-2 mt-1">
            <span class="fw-bold" style="font-size:15px;">
                @(_isNew ? "Nuevo colaborador" : _form.FullName())
            </span>
            @if (!_isNew)
            {
                <span class="ns-badge-@(_form.IsActive ? "active" : "inactive")">
                    @(_form.IsActive ? "Activo" : "Inactivo")
                </span>
                <small class="text-muted" style="font-size:12px;">No. @_form.EmployeeNumber &middot; @_form.Code</small>
            }
        </div>
    </div>
    <div class="d-flex gap-2 align-items-center">
        <button class="btn btn-outline-secondary btn-sm px-3" @onclick='() => Nav.NavigateTo("/human-resources/employees")'>
            <span class="bi bi-x-lg me-1"></span>Cancelar
        </button>
        <button class="btn btn-primary btn-sm px-4" @onclick="SaveAsync" disabled="@_saving">
            @if (_saving)
            {
                <span class="spinner-border spinner-border-sm me-1"></span>
                <span>Guardando…</span>
            }
            else
            {
                <span class="bi bi-check2 me-1"></span>
                <span>Guardar</span>
            }
        </button>
    </div>
</div>

<div style="max-width:1120px;margin:0 auto;padding:20px 16px 40px 16px;">

    @if (!string.IsNullOrEmpty(_error))
    {
        <div class="alert alert-danger alert-dismissible d-flex align-items-center gap-2 mb-3 py-2" style="font-size:13px;">
            <span class="bi bi-exclamation-triangle-fill"></span>
            <span class="flex-grow-1">@_error</span>
            <button type="button" class="btn-close" @onclick='() => _error = string.Empty'></button>
        </div>
    }
    @if (!string.IsNullOrEmpty(_success))
    {
        <div class="alert alert-success alert-dismissible d-flex align-items-center gap-2 mb-3 py-2" style="font-size:13px;">
            <span class="bi bi-check-circle-fill"></span>
            <span class="flex-grow-1">@_success</span>
            <button type="button" class="btn-close" @onclick='() => _success = string.Empty'></button>
        </div>
    }

    @if (_loading)
    {
        <div class="text-center py-5">
            <div class="spinner-border text-primary mb-2" style="width:2.5rem;height:2.5rem;"></div>
            <p class="text-muted" style="font-size:13px;">Cargando datos del colaborador…</p>
        </div>
    }
    else
    {
        <!-- ══ TABS PILLS ══ -->
        <ul class="nav ns-tab-nav mb-4">
            <li class="nav-item">
                <button class="nav-link @(_activeTab == 0 ? "active" : "")" @onclick="() => _activeTab = 0">
                    <span class="bi bi-person-badge"></span> Identificación
                </button>
            </li>
            <li class="nav-item">
                <button class="nav-link @(_activeTab == 1 ? "active" : "")" @onclick="() => _activeTab = 1">
                    <span class="bi bi-person-lines-fill"></span> Datos personales
                </button>
            </li>
            <li class="nav-item">
                <button class="nav-link @(_activeTab == 2 ? "active" : "")" @onclick="() => _activeTab = 2">
                    <span class="bi bi-house-door"></span> Domicilio
                </button>
            </li>
            <li class="nav-item">
                <button class="nav-link @(_activeTab == 3 ? "active" : "")" @onclick="() => _activeTab = 3">
                    <span class="bi bi-briefcase"></span> Laboral
                </button>
            </li>
            <li class="nav-item">
                <button class="nav-link @(_activeTab == 4 ? "active" : "")" @onclick="() => _activeTab = 4">
                    <span class="bi bi-shield-check"></span> IMSS / SAT
                </button>
            </li>
            <li class="nav-item">
                <button class="nav-link @(_activeTab == 5 ? "active" : "")" @onclick="() => _activeTab = 5">
                    <span class="bi bi-bank"></span> Banco
                </button>
            </li>
        </ul>

        <!-- ════════════════════════════════════════ -->
        <!-- TAB 0: IDENTIFICACIÓN                    -->
        <!-- ════════════════════════════════════════ -->
        @if (_activeTab == 0)
        {
            <!-- Empresa y Organización -->
            <div class="ns-section ns-blue">
                <div class="ns-section-header">
                    <span class="bi bi-building"></span> Empresa y Organización
                </div>
                <div class="ns-section-body">
                    <div class="row g-3">
                        <div class="col-12 col-md-6">
                            <label class="ns-field-label">Empresa <span class="text-danger">*</span></label>
                            <select class="form-select ns-form-input" @bind="_form.CompanyId" @bind:after="OnCompanyChanged">
                                <option value="">— Selecciona empresa —</option>
                                @foreach (var c in _companies)
                                {
                                    <option value="@c.CompanyId">@c.Name</option>
                                }
                            </select>
                        </div>
                        <div class="col-12 col-md-6">
                            <label class="ns-field-label">Sucursal</label>
                            <select class="form-select ns-form-input" @bind="_form.BranchId">
                                <option value="">— Ninguna —</option>
                                @foreach (var b in _branches)
                                {
                                    <option value="@b.BranchId">@b.Name</option>
                                }
                            </select>
                        </div>
                    </div>
                </div>
            </div>

            <!-- Asignación -->
            <div class="ns-section ns-green">
                <div class="ns-section-header">
                    <span class="bi bi-diagram-3"></span> Asignación
                </div>
                <div class="ns-section-body">
                    <div class="row g-3">
                        <div class="col-12 col-md-4">
                            <label class="ns-field-label">Departamento</label>
                            <div class="ns-fk-group">
                                <select class="form-select ns-form-input" @bind="_form.DepartmentId">
                                    <option value="">— Ninguno —</option>
                                    @foreach (var d in _departments)
                                    {
                                        <option value="@d.DepartmentId">@d.Name</option>
                                    }
                                </select>
                                <button type="button" class="ns-plus-btn" title="Crear departamento" @onclick="() => OpenDeptModal()">+</button>
                            </div>
                        </div>
                        <div class="col-12 col-md-4">
                            <label class="ns-field-label">Puesto</label>
                            <div class="ns-fk-group">
                                <select class="form-select ns-form-input" @bind="_form.PositionId">
                                    <option value="">— Ninguno —</option>
                                    @foreach (var p in _positions)
                                    {
                                        <option value="@p.PositionId">@p.Name</option>
                                    }
                                </select>
                                <button type="button" class="ns-plus-btn" title="Crear puesto" @onclick="() => OpenPosModal()">+</button>
                            </div>
                        </div>
                        <div class="col-12 col-md-4">
                            <label class="ns-field-label">Horario</label>
                            <div class="ns-fk-group">
                                <select class="form-select ns-form-input" @bind="_form.WorkScheduleId">
                                    <option value="">— Ninguno —</option>
                                    @foreach (var w in _schedules)
                                    {
                                        <option value="@w.WorkScheduleId">@w.Name</option>
                                    }
                                </select>
                                <button type="button" class="ns-plus-btn" title="Crear horario" @onclick="() => OpenSchedModal()">+</button>
                            </div>
                        </div>
                    </div>
                </div>
            </div>

            <!-- Identificadores del sistema -->
            <div class="ns-section ns-slate">
                <div class="ns-section-header">
                    <span class="bi bi-hash"></span> Identificadores del sistema
                </div>
                <div class="ns-section-body">
                    <div class="row g-3">
                        <div class="col-6 col-md-3">
                            <label class="ns-field-label">No. empleado <span class="text-danger">*</span></label>
                            <input class="form-control ns-form-input" @bind="_form.EmployeeNumber" placeholder="0001" />
                        </div>
                        <div class="col-6 col-md-3">
                            <label class="ns-field-label">Código <span class="text-danger">*</span></label>
                            <input class="form-control ns-form-input" @bind="_form.Code" placeholder="EMP-001" />
                        </div>
                        <div class="col-6 col-md-3">
                            <label class="ns-field-label">Clave reloj</label>
                            <input class="form-control ns-form-input" @bind="_form.ClockKey" placeholder="001" />
                        </div>
                        <div class="col-6 col-md-3">
                            <label class="ns-field-label">Clave NOI</label>
                            <input class="form-control ns-form-input" @bind="_form.NoiKey" placeholder="001" />
                        </div>
                    </div>
                </div>
            </div>

            <!-- Estado -->
            <div class="ns-section ns-slate">
                <div class="ns-section-header">
                    <span class="bi bi-toggle-on"></span> Estado
                </div>
                <div class="ns-section-body">
                    <div class="row g-3 align-items-center">
                        <div class="col-12 col-md-3">
                            <label class="ns-field-label">Estatus</label>
                            <select class="form-select ns-form-input" @bind="_form.Status">
                                <option value="active">Activo</option>
                                <option value="inactive">Inactivo</option>
                                <option value="suspended">Suspendido</option>
                                <option value="terminated">Baja</option>
                            </select>
                        </div>
                        <div class="col-auto">
                            <label class="ns-field-label">&nbsp;</label>
                            <div class="form-check form-switch pt-1">
                                <input class="form-check-input" type="checkbox" id="chkActive" @bind="_form.IsActive" />
                                <label class="form-check-label" for="chkActive" style="font-size:13px;">Activo en sistema</label>
                            </div>
                        </div>
                        <div class="col-auto">
                            <label class="ns-field-label">&nbsp;</label>
                            <div class="form-check form-switch pt-1">
                                <input class="form-check-input" type="checkbox" id="chkRecibo" @bind="_form.PrintReceipt" />
                                <label class="form-check-label" for="chkRecibo" style="font-size:13px;">Imprime recibo</label>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        }

        <!-- ════════════════════════════════════════ -->
        <!-- TAB 1: DATOS PERSONALES                  -->
        <!-- ════════════════════════════════════════ -->
        @if (_activeTab == 1)
        {
            <!-- Nombre completo -->
            <div class="ns-section ns-purple">
                <div class="ns-section-header">
                    <span class="bi bi-person-vcard"></span> Nombre completo
                </div>
                <div class="ns-section-body">
                    <div class="row g-3">
                        <div class="col-12 col-md-3">
                            <label class="ns-field-label">Apellido paterno <span class="text-danger">*</span></label>
                            <input class="form-control ns-form-input" @bind="_form.LastName" placeholder="García" />
                        </div>
                        <div class="col-12 col-md-3">
                            <label class="ns-field-label">Apellido materno</label>
                            <input class="form-control ns-form-input" @bind="_form.SecondLastName" placeholder="López" />
                        </div>
                        <div class="col-12 col-md-3">
                            <label class="ns-field-label">Nombre(s) <span class="text-danger">*</span></label>
                            <input class="form-control ns-form-input" @bind="_form.FirstName" placeholder="Juan" />
                        </div>
                        <div class="col-12 col-md-3">
                            <label class="ns-field-label">Segundo nombre</label>
                            <input class="form-control ns-form-input" @bind="_form.MiddleName" placeholder="Carlos" />
                        </div>
                    </div>
                </div>
            </div>

            <!-- Datos personales -->
            <div class="ns-section ns-green">
                <div class="ns-section-header">
                    <span class="bi bi-info-circle"></span> Datos personales
                </div>
                <div class="ns-section-body">
                    <div class="row g-3">
                        <div class="col-6 col-md-3">
                            <label class="ns-field-label">F. nacimiento</label>
                            <input type="date" class="form-control ns-form-input" @bind="_form.BirthDate" />
                        </div>
                        <div class="col-6 col-md-2">
                            <label class="ns-field-label">Sexo</label>
                            <select class="form-select ns-form-input" @bind="_form.Gender">
                                <option value="">—</option>
                                <option value="M">Masculino</option>
                                <option value="F">Femenino</option>
                            </select>
                        </div>
                        <div class="col-6 col-md-3">
                            <label class="ns-field-label">Estado civil</label>
                            <select class="form-select ns-form-input" @bind="_form.MaritalStatus">
                                <option value="">—</option>
                                <option value="soltero">Soltero(a)</option>
                                <option value="casado">Casado(a)</option>
                                <option value="divorciado">Divorciado(a)</option>
                                <option value="viudo">Viudo(a)</option>
                                <option value="union">Unión libre</option>
                            </select>
                        </div>
                        <div class="col-6 col-md-2">
                            <label class="ns-field-label">Tipo sangre</label>
                            <select class="form-select ns-form-input" @bind="_form.BloodType">
                                <option value="">—</option>
                                @foreach (var bt in new[] { "A+", "A-", "B+", "B-", "AB+", "AB-", "O+", "O-" })
                                {
                                    <option value="@bt">@bt</option>
                                }
                            </select>
                        </div>
                        <div class="col-12 col-md-5">
                            <label class="ns-field-label">Lugar nacimiento</label>
                            <input class="form-control ns-form-input" @bind="_form.PlaceOfBirth" placeholder="Chihuahua, Chih." />
                        </div>
                        <div class="col-12 col-md-3">
                            <label class="ns-field-label">Nacionalidad</label>
                            <input class="form-control ns-form-input" @bind="_form.Nationality" placeholder="Mexicana" />
                        </div>
                    </div>
                </div>
            </div>

            <!-- Contacto -->
            <div class="ns-section ns-cyan">
                <div class="ns-section-header">
                    <span class="bi bi-telephone"></span> Contacto
                </div>
                <div class="ns-section-body">
                    <div class="row g-3">
                        <div class="col-12 col-md-5">
                            <label class="ns-field-label">Email</label>
                            <input type="email" class="form-control ns-form-input" @bind="_form.Email" placeholder="nombre@empresa.com" />
                        </div>
                        <div class="col-6 col-md-3">
                            <label class="ns-field-label">Teléfono</label>
                            <input class="form-control ns-form-input" @bind="_form.Phone" placeholder="614 123 4567" />
                        </div>
                        <div class="col-6 col-md-3">
                            <label class="ns-field-label">Tel. emergencia</label>
                            <input class="form-control ns-form-input" @bind="_form.EmergencyPhone" placeholder="614 987 6543" />
                        </div>
                    </div>
                </div>
            </div>

            <!-- Familia -->
            <div class="ns-section ns-orange">
                <div class="ns-section-header">
                    <span class="bi bi-people"></span> Familia
                </div>
                <div class="ns-section-body">
                    <div class="row g-3">
                        <div class="col-12 col-md-5">
                            <label class="ns-field-label">Nombre del padre</label>
                            <input class="form-control ns-form-input" @bind="_form.FatherName" placeholder="Nombre completo del padre" />
                        </div>
                        <div class="col-12 col-md-5">
                            <label class="ns-field-label">Nombre de la madre</label>
                            <input class="form-control ns-form-input" @bind="_form.MotherName" placeholder="Nombre completo de la madre" />
                        </div>
                    </div>
                </div>
            </div>
        }

        <!-- ════════════════════════════════════════ -->
        <!-- TAB 2: DOMICILIO                         -->
        <!-- ════════════════════════════════════════ -->
        @if (_activeTab == 2)
        {
            <div class="ns-section ns-orange">
                <div class="ns-section-header">
                    <span class="bi bi-geo-alt"></span> Domicilio
                </div>
                <div class="ns-section-body">
                    <div class="row g-3">
                        <div class="col-12">
                            <label class="ns-field-label">Calle y número</label>
                            <input class="form-control ns-form-input" @bind="_form.AddressStreet" placeholder="Av. Independencia 456 Interior 3" />
                        </div>
                        <div class="col-12 col-md-4">
                            <label class="ns-field-label">Colonia</label>
                            <input class="form-control ns-form-input" @bind="_form.AddressColony" placeholder="Col. Centro" />
                        </div>
                        <div class="col-12 col-md-4">
                            <label class="ns-field-label">Ciudad / Municipio</label>
                            <input class="form-control ns-form-input" @bind="_form.AddressCity" placeholder="Chihuahua" />
                        </div>
                        <div class="col-12 col-md-3">
                            <label class="ns-field-label">Estado</label>
                            <input class="form-control ns-form-input" @bind="_form.AddressState" placeholder="Chihuahua" />
                        </div>
                        <div class="col-6 col-md-2">
                            <label class="ns-field-label">C.P.</label>
                            <input class="form-control ns-form-input" @bind="_form.AddressZipCode" maxlength="5" placeholder="31000" />
                        </div>
                    </div>
                </div>
            </div>
        }

        <!-- ════════════════════════════════════════ -->
        <!-- TAB 3: LABORAL                           -->
        <!-- ════════════════════════════════════════ -->
        @if (_activeTab == 3)
        {
            <!-- Contratación -->
            <div class="ns-section ns-purple">
                <div class="ns-section-header">
                    <span class="bi bi-file-earmark-person"></span> Contratación
                </div>
                <div class="ns-section-body">
                    <div class="row g-3">
                        <div class="col-6 col-md-3">
                            <label class="ns-field-label">F. ingreso <span class="text-danger">*</span></label>
                            <input type="date" class="form-control ns-form-input" @bind="_form.HireDate" />
                        </div>
                        <div class="col-6 col-md-3">
                            <label class="ns-field-label">Tipo contrato</label>
                            <select class="form-select ns-form-input" @bind="_form.ContractType">
                                <option value="indefinite">Indeterminado</option>
                                <option value="determined">Determinado</option>
                                <option value="construction_temp">Obra determinada</option>
                                <option value="field_temp">Temporada</option>
                                <option value="trial">A prueba</option>
                                <option value="training">Capacitación</option>
                            </select>
                        </div>
                        <div class="col-6 col-md-3">
                            <label class="ns-field-label">Tipo empleado</label>
                            <select class="form-select ns-form-input" @bind="_form.EmployeeType">
                                <option value="base">Base</option>
                                <option value="confianza">Confianza</option>
                                <option value="eventual">Eventual</option>
                            </select>
                        </div>
                        <div class="col-6 col-md-3">
                            <label class="ns-field-label">Periodo nómina</label>
                            <select class="form-select ns-form-input" @bind="_form.PayrollPeriodType">
                                <option value="semanal">Semanal</option>
                                <option value="decenal">Decenal</option>
                                <option value="quincenal">Quincenal</option>
                                <option value="mensual">Mensual</option>
                            </select>
                        </div>
                        <div class="col-12 col-md-4">
                            <label class="ns-field-label">Jefe directo</label>
                            <input class="form-control ns-form-input" @bind="_form.ImmediateSupervisor" placeholder="Nombre del supervisor" />
                        </div>
                        <div class="col-12 col-md-3">
                            <label class="ns-field-label">Categoría</label>
                            <input class="form-control ns-form-input" @bind="_form.Category" placeholder="Ej: Operario A" />
                        </div>
                    </div>
                </div>
            </div>

            <!-- Salario -->
            <div class="ns-section ns-green">
                <div class="ns-section-header">
                    <span class="bi bi-cash-coin"></span> Salario
                </div>
                <div class="ns-section-body">
                    <div class="row g-3">
                        <div class="col-6 col-md-3">
                            <label class="ns-field-label">Salario diario</label>
                            <div class="input-group input-group-sm">
                                <span class="input-group-text">$</span>
                                <input type="number" step="0.01" class="form-control ns-form-input" @bind="_form.DailySalary" placeholder="0.00" />
                            </div>
                        </div>
                        <div class="col-6 col-md-3">
                            <label class="ns-field-label">SDI (diario integrado)</label>
                            <div class="input-group input-group-sm">
                                <span class="input-group-text">$</span>
                                <input type="number" step="0.01" class="form-control ns-form-input" @bind="_form.IntegratedDailySalary" placeholder="0.00" />
                            </div>
                        </div>
                        <div class="col-6 col-md-3">
                            <label class="ns-field-label">SBC Fija</label>
                            <div class="input-group input-group-sm">
                                <span class="input-group-text">$</span>
                                <input type="number" step="0.01" class="form-control ns-form-input" @bind="_form.SbcFija" placeholder="0.00" />
                            </div>
                        </div>
                        <div class="col-6 col-md-2">
                            <label class="ns-field-label">Zona salarial</label>
                            <select class="form-select ns-form-input" @bind="_form.SalaryZone">
                                <option value="A">Zona A</option>
                                <option value="B">Zona B</option>
                                <option value="C">Zona C</option>
                            </select>
                        </div>
                    </div>
                </div>
            </div>

            <!-- Baja -->
            <div class="ns-section ns-rose">
                <div class="ns-section-header">
                    <span class="bi bi-door-open"></span> Baja (si aplica)
                </div>
                <div class="ns-section-body">
                    <div class="row g-3">
                        <div class="col-6 col-md-3">
                            <label class="ns-field-label">Fecha de baja</label>
                            <input type="date" class="form-control ns-form-input" @bind="_form.TerminationDate" />
                        </div>
                        <div class="col-12 col-md-5">
                            <label class="ns-field-label">Motivo de baja</label>
                            <input class="form-control ns-form-input" @bind="_form.TerminationReason" placeholder="Ej: Renuncia voluntaria" />
                        </div>
                        <div class="col-6 col-md-3">
                            <label class="ns-field-label">F. reingreso</label>
                            <input type="date" class="form-control ns-form-input" @bind="_form.ReentryDate" />
                        </div>
                    </div>
                </div>
            </div>

            <!-- Notas -->
            <div class="ns-section ns-slate">
                <div class="ns-section-header">
                    <span class="bi bi-journal-text"></span> Notas / Expediente
                </div>
                <div class="ns-section-body">
                    <textarea class="form-control ns-form-input" rows="4" @bind="_form.Notes" placeholder="Observaciones, notas de expediente, acuerdos especiales…"></textarea>
                </div>
            </div>
        }

        <!-- ════════════════════════════════════════ -->
        <!-- TAB 4: IMSS / SAT                        -->
        <!-- ════════════════════════════════════════ -->
        @if (_activeTab == 4)
        {
            <!-- Documentos fiscales -->
            <div class="ns-section ns-amber">
                <div class="ns-section-header">
                    <span class="bi bi-file-earmark-text"></span> Documentos fiscales
                </div>
                <div class="ns-section-body">
                    <div class="row g-3">
                        <div class="col-12 col-md-4">
                            <label class="ns-field-label">RFC</label>
                            <input class="form-control ns-form-input text-uppercase" @bind="_form.TaxId" maxlength="13" placeholder="XAXX010101000" />
                        </div>
                        <div class="col-12 col-md-5">
                            <label class="ns-field-label">CURP</label>
                            <input class="form-control ns-form-input text-uppercase" @bind="_form.Curp" maxlength="18" placeholder="XAXX010101MXXXXX00" />
                        </div>
                        <div class="col-12 col-md-3">
                            <label class="ns-field-label">NSS</label>
                            <input class="form-control ns-form-input" @bind="_form.Nss" maxlength="11" placeholder="12345678901" />
                        </div>
                        <div class="col-12 col-md-4">
                            <label class="ns-field-label">ID nacional</label>
                            <input class="form-control ns-form-input" @bind="_form.NationalId" placeholder="Número de identificación" />
                        </div>
                    </div>
                </div>
            </div>

            <!-- IMSS -->
            <div class="ns-section ns-blue">
                <div class="ns-section-header">
                    <span class="bi bi-hospital"></span> IMSS
                </div>
                <div class="ns-section-body">
                    <div class="row g-3 align-items-end">
                        <div class="col-12 col-md-4">
                            <label class="ns-field-label">Reg. patronal</label>
                            <input class="form-control ns-form-input" @bind="_form.ImssRegId" placeholder="B123456789" />
                        </div>
                        <div class="col-6 col-md-3">
                            <label class="ns-field-label">F. alta IMSS</label>
                            <input type="date" class="form-control ns-form-input" @bind="_form.ImssRegistrationDate" />
                        </div>
                        <div class="col-6 col-md-3">
                            <label class="ns-field-label">F. baja IMSS</label>
                            <input type="date" class="form-control ns-form-input" @bind="_form.ImssTerminationDate" />
                        </div>
                        <div class="col-6 col-md-2">
                            <label class="ns-field-label">UMF</label>
                            <input class="form-control ns-form-input" @bind="_form.Umf" maxlength="10" placeholder="068" />
                        </div>
                        <div class="col-auto">
                            <div class="form-check form-switch">
                                <input class="form-check-input" type="checkbox" id="chkImss" @bind="_form.IsImssRegistered" />
                                <label class="form-check-label" for="chkImss" style="font-size:13px;">Registrado en IMSS</label>
                            </div>
                        </div>
                    </div>
                </div>
            </div>

            <!-- Configuración fiscal -->
            <div class="ns-section ns-purple">
                <div class="ns-section-header">
                    <span class="bi bi-receipt"></span> Configuración fiscal
                </div>
                <div class="ns-section-body">
                    <div class="row g-3">
                        <div class="col-12 col-md-5">
                            <label class="ns-field-label">Régimen fiscal SAT</label>
                            <select class="form-select ns-form-input" @bind="_form.TaxRegime">
                                <option value="sueldos_salarios">Sueldos y Salarios</option>
                                <option value="asimilados">Asimilados a Salarios</option>
                            </select>
                        </div>
                        <div class="col-12 col-md-4">
                            <label class="ns-field-label">Base cotización</label>
                            <select class="form-select ns-form-input" @bind="_form.CotizationBase">
                                <option value="fixed">Fija</option>
                                <option value="variable">Variable</option>
                                <option value="mixed">Mixta</option>
                            </select>
                        </div>
                    </div>
                </div>
            </div>

            <!-- Fondos y créditos -->
            <div class="ns-section ns-green">
                <div class="ns-section-header">
                    <span class="bi bi-piggy-bank"></span> Fondos y créditos
                </div>
                <div class="ns-section-body">
                    <div class="row g-3">
                        <div class="col-12 col-md-3">
                            <label class="ns-field-label">AFORE</label>
                            <input class="form-control ns-form-input" @bind="_form.Afore" placeholder="Nombre de la AFORE" />
                        </div>
                        <div class="col-12 col-md-3">
                            <label class="ns-field-label">No. FONACOT</label>
                            <input class="form-control ns-form-input" @bind="_form.Fonacot" placeholder="Número de crédito" />
                        </div>
                        <div class="col-12 col-md-4">
                            <label class="ns-field-label">Crédito INFONAVIT</label>
                            <input class="form-control ns-form-input" @bind="_form.Infonavit" placeholder="Número de crédito INFONAVIT" />
                        </div>
                    </div>
                </div>
            </div>
        }

        <!-- ════════════════════════════════════════ -->
        <!-- TAB 5: BANCO                             -->
        <!-- ════════════════════════════════════════ -->
        @if (_activeTab == 5)
        {
            <div class="ns-section ns-cyan">
                <div class="ns-section-header">
                    <span class="bi bi-credit-card"></span> Datos bancarios
                </div>
                <div class="ns-section-body">
                    <div class="row g-3">
                        <div class="col-12 col-md-4">
                            <label class="ns-field-label">Forma de pago</label>
                            <select class="form-select ns-form-input" @bind="_form.PaymentForm">
                                <option value="tarjeta">Tarjeta bancaria</option>
                                <option value="transferencia">Transferencia</option>
                                <option value="efectivo">Efectivo</option>
                                <option value="cheque">Cheque</option>
                            </select>
                        </div>
                        <div class="col-12 col-md-4">
                            <label class="ns-field-label">Banco</label>
                            <div class="ns-fk-group">
                                <select class="form-select ns-form-input" @bind="_form.BankCode">
                                    <option value="">— Selecciona banco —</option>
                                    @foreach (var b in _banks)
                                    {
                                        <option value="@b.Code">@b.ShortName — @b.Name</option>
                                    }
                                </select>
                                <button type="button" class="ns-plus-btn" title="Registrar banco" @onclick="() => OpenBankModal()">+</button>
                            </div>
                        </div>
                        <div class="col-12 col-md-4">
                            <label class="ns-field-label">Sucursal bancaria</label>
                            <input class="form-control ns-form-input" @bind="_form.BankBranch" placeholder="Sucursal o plaza" />
                        </div>
                        <div class="col-12 col-md-5">
                            <label class="ns-field-label">Número de cuenta</label>
                            <input class="form-control ns-form-input" @bind="_form.BankAccount" placeholder="1234567890" />
                        </div>
                        <div class="col-12 col-md-7">
                            <label class="ns-field-label">CLABE interbancaria (18 dígitos)</label>
                            <input class="form-control ns-form-input" @bind="_form.Clabe" maxlength="18" placeholder="012 345 678901234567" />
                        </div>
                    </div>
                </div>
            </div>
        }

        <!-- ── Botón inferior guardar ── -->
        <div class="d-flex justify-content-end mt-4 gap-2">
            <button class="btn btn-outline-secondary px-4" @onclick='() => Nav.NavigateTo("/human-resources/employees")'>
                <span class="bi bi-x-lg me-1"></span> Cancelar
            </button>
            <button class="btn btn-primary px-5" @onclick="SaveAsync" disabled="@_saving">
                @if (_saving)
                {
                    <span class="spinner-border spinner-border-sm me-1"></span>
                    <span>Guardando…</span>
                }
                else
                {
                    <span class="bi bi-check2 me-1"></span>
                    <span>Guardar cambios</span>
                }
            </button>
        </div>
    }
</div>

<!-- ═══════════ MODAL CREAR DEPARTAMENTO ═══════════ -->
@if (_showDeptModal)
{
    <div class="modal fade show d-block" style="background:rgba(0,0,0,.45);" tabindex="-1">
        <div class="modal-dialog modal-dialog-centered">
            <div class="modal-content" style="border-radius:10px;border:none;box-shadow:0 20px 60px rgba(0,0,0,.2);">
                <div class="modal-header" style="background:#F0FDF4;border-bottom:1px solid #bbf7d0;border-radius:10px 10px 0 0;">
                    <div class="d-flex align-items-center gap-2">
                        <span class="bi bi-diagram-3 text-success fs-5"></span>
                        <h6 class="modal-title mb-0 fw-bold">Nuevo departamento</h6>
                    </div>
                    <button type="button" class="btn-close" @onclick="() => _showDeptModal = false"></button>
                </div>
                <div class="modal-body p-4">
                    <div class="mb-3">
                        <label class="ns-field-label">Código</label>
                        <input class="form-control ns-form-input" @bind="_newDept.Code" placeholder="PROD" />
                    </div>
                    <div class="mb-3">
                        <label class="ns-field-label">Nombre</label>
                        <input class="form-control ns-form-input" @bind="_newDept.Name" placeholder="Producción" />
                    </div>
                    @if (!string.IsNullOrEmpty(_deptError))
                    {
                        <div class="alert alert-danger py-2 mb-0" style="font-size:13px;">@_deptError</div>
                    }
                </div>
                <div class="modal-footer" style="border-top:1px solid #e2e8f0;">
                    <button class="btn btn-outline-secondary btn-sm" @onclick="() => _showDeptModal = false">Cancelar</button>
                    <button class="btn btn-success btn-sm px-4" @onclick="SaveDeptAsync" disabled="@_savingModal">
                        @if (_savingModal) { <span class="spinner-border spinner-border-sm me-1"></span> }
                        Crear departamento
                    </button>
                </div>
            </div>
        </div>
    </div>
}

<!-- ═══════════ MODAL CREAR PUESTO ═══════════ -->
@if (_showPosModal)
{
    <div class="modal fade show d-block" style="background:rgba(0,0,0,.45);" tabindex="-1">
        <div class="modal-dialog modal-dialog-centered">
            <div class="modal-content" style="border-radius:10px;border:none;box-shadow:0 20px 60px rgba(0,0,0,.2);">
                <div class="modal-header" style="background:#F5F3FF;border-bottom:1px solid #ddd6fe;border-radius:10px 10px 0 0;">
                    <div class="d-flex align-items-center gap-2">
                        <span class="bi bi-person-gear text-purple fs-5" style="color:#6D28D9;"></span>
                        <h6 class="modal-title mb-0 fw-bold">Nuevo puesto</h6>
                    </div>
                    <button type="button" class="btn-close" @onclick="() => _showPosModal = false"></button>
                </div>
                <div class="modal-body p-4">
                    <div class="mb-3">
                        <label class="ns-field-label">Código</label>
                        <input class="form-control ns-form-input" @bind="_newPos.Code" placeholder="OPER" />
                    </div>
                    <div class="mb-3">
                        <label class="ns-field-label">Nombre</label>
                        <input class="form-control ns-form-input" @bind="_newPos.Name" placeholder="Operario" />
                    </div>
                    <div class="mb-3">
                        <label class="ns-field-label">Departamento</label>
                        <select class="form-select ns-form-input" @bind="_newPos.DepartmentId">
                            <option value="">— Ninguno —</option>
                            @foreach (var d in _departments)
                            {
                                <option value="@d.DepartmentId">@d.Name</option>
                            }
                        </select>
                    </div>
                    @if (!string.IsNullOrEmpty(_posError))
                    {
                        <div class="alert alert-danger py-2 mb-0" style="font-size:13px;">@_posError</div>
                    }
                </div>
                <div class="modal-footer" style="border-top:1px solid #e2e8f0;">
                    <button class="btn btn-outline-secondary btn-sm" @onclick="() => _showPosModal = false">Cancelar</button>
                    <button class="btn btn-primary btn-sm px-4" @onclick="SavePosAsync" disabled="@_savingModal">
                        @if (_savingModal) { <span class="spinner-border spinner-border-sm me-1"></span> }
                        Crear puesto
                    </button>
                </div>
            </div>
        </div>
    </div>
}

<!-- ═══════════ MODAL CREAR HORARIO ═══════════ -->
@if (_showSchedModal)
{
    <div class="modal fade show d-block" style="background:rgba(0,0,0,.45);" tabindex="-1">
        <div class="modal-dialog modal-dialog-centered">
            <div class="modal-content" style="border-radius:10px;border:none;box-shadow:0 20px 60px rgba(0,0,0,.2);">
                <div class="modal-header" style="background:#FFFBEB;border-bottom:1px solid #fde68a;border-radius:10px 10px 0 0;">
                    <div class="d-flex align-items-center gap-2">
                        <span class="bi bi-clock fs-5" style="color:#D97706;"></span>
                        <h6 class="modal-title mb-0 fw-bold">Nuevo horario</h6>
                    </div>
                    <button type="button" class="btn-close" @onclick="() => _showSchedModal = false"></button>
                </div>
                <div class="modal-body p-4">
                    <div class="mb-3">
                        <label class="ns-field-label">Código</label>
                        <input class="form-control ns-form-input" @bind="_newSched.Code" placeholder="L-V-8" />
                    </div>
                    <div class="mb-3">
                        <label class="ns-field-label">Nombre</label>
                        <input class="form-control ns-form-input" @bind="_newSched.Name" placeholder="Lunes a Viernes 8h" />
                    </div>
                    <div class="mb-3">
                        <label class="ns-field-label">Horas semanales</label>
                        <input type="number" class="form-control ns-form-input" @bind="_newSched.WeeklyHours" placeholder="40" />
                    </div>
                    @if (!string.IsNullOrEmpty(_schedError))
                    {
                        <div class="alert alert-danger py-2 mb-0" style="font-size:13px;">@_schedError</div>
                    }
                </div>
                <div class="modal-footer" style="border-top:1px solid #e2e8f0;">
                    <button class="btn btn-outline-secondary btn-sm" @onclick="() => _showSchedModal = false">Cancelar</button>
                    <button class="btn btn-warning btn-sm px-4" @onclick="SaveSchedAsync" disabled="@_savingModal">
                        @if (_savingModal) { <span class="spinner-border spinner-border-sm me-1"></span> }
                        Crear horario
                    </button>
                </div>
            </div>
        </div>
    </div>
}

<!-- ═══════════ MODAL CREAR BANCO ═══════════ -->
@if (_showBankModal)
{
    <div class="modal fade show d-block" style="background:rgba(0,0,0,.45);" tabindex="-1">
        <div class="modal-dialog modal-dialog-centered">
            <div class="modal-content" style="border-radius:10px;border:none;box-shadow:0 20px 60px rgba(0,0,0,.2);">
                <div class="modal-header" style="background:#F0F9FF;border-bottom:1px solid #bae6fd;border-radius:10px 10px 0 0;">
                    <div class="d-flex align-items-center gap-2">
                        <span class="bi bi-bank fs-5" style="color:#0369A1;"></span>
                        <h6 class="modal-title mb-0 fw-bold">Registrar banco</h6>
                    </div>
                    <button type="button" class="btn-close" @onclick="() => _showBankModal = false"></button>
                </div>
                <div class="modal-body p-4">
                    <div class="mb-3">
                        <label class="ns-field-label">Código</label>
                        <input class="form-control ns-form-input" @bind="_newBank.Code" placeholder="002" />
                    </div>
                    <div class="mb-3">
                        <label class="ns-field-label">Nombre corto</label>
                        <input class="form-control ns-form-input" @bind="_newBank.ShortName" placeholder="BBVA" />
                    </div>
                    <div class="mb-3">
                        <label class="ns-field-label">Nombre completo</label>
                        <input class="form-control ns-form-input" @bind="_newBank.Name" placeholder="BBVA Bancomer" />
                    </div>
                    <div class="mb-3">
                        <label class="ns-field-label">Código SAT <span class="text-muted fw-normal">(opcional)</span></label>
                        <input class="form-control ns-form-input" @bind="_newBank.SatCode" placeholder="002" />
                    </div>
                    @if (!string.IsNullOrEmpty(_bankError))
                    {
                        <div class="alert alert-danger py-2 mb-0" style="font-size:13px;">@_bankError</div>
                    }
                </div>
                <div class="modal-footer" style="border-top:1px solid #e2e8f0;">
                    <button class="btn btn-outline-secondary btn-sm" @onclick="() => _showBankModal = false">Cancelar</button>
                    <button class="btn btn-sm px-4" style="background:#0369A1;color:#fff;" @onclick="SaveBankAsync" disabled="@_savingModal">
                        @if (_savingModal) { <span class="spinner-border spinner-border-sm me-1"></span> }
                        Registrar banco
                    </button>
                </div>
            </div>
        </div>
    </div>
}

@code {
    [Parameter] public Guid? EmployeeId { get; set; }

    private int _activeTab;
    private bool _loading = true;
    private bool _saving;
    private bool _savingModal;
    private string _error = string.Empty;
    private string _success = string.Empty;
    private bool _isNew => !EmployeeId.HasValue || EmployeeId == Guid.Empty;

    private EmployeeFormModel _form = new();

    // Lookups
    private List<CompanyItem> _companies = [];
    private List<BranchItem> _branches = [];
    private List<DeptItem> _departments = [];
    private List<PosItem> _positions = [];
    private List<SchedItem> _schedules = [];
    private List<BankItem> _banks = [];

    // Quick-add modals
    private bool _showDeptModal, _showPosModal, _showSchedModal, _showBankModal;
    private string _deptError = string.Empty, _posError = string.Empty, _schedError = string.Empty, _bankError = string.Empty;
    private NewDeptForm _newDept = new();
    private NewPosForm _newPos = new();
    private NewSchedForm _newSched = new();
    private NewBankForm _newBank = new();

    protected override async Task OnInitializedAsync()
    {
        var client = HttpClientFactory.CreateClient("Nanchesoft.Api");

        var companiesTask = client.GetFromJsonAsync<List<CompanyItem>>("/api/organization/companies");
        var branchesTask = client.GetFromJsonAsync<List<BranchItem>>("/api/organization/branches");
        var deptsTask = client.GetFromJsonAsync<List<DeptItem>>("/api/hr/departments");
        var posTask = client.GetFromJsonAsync<List<PosItem>>("/api/hr/positions");
        var schedsTask = client.GetFromJsonAsync<List<SchedItem>>("/api/hr/work-schedules");
        var banksTask = client.GetFromJsonAsync<List<BankItem>>("/api/hr/banks");

        await Task.WhenAll(companiesTask, branchesTask, deptsTask, posTask, schedsTask, banksTask);

        _companies = (await companiesTask)?.Where(x => x.IsActive).OrderBy(x => x.Name).ToList() ?? [];
        _branches = (await branchesTask)?.Where(x => x.IsActive).OrderBy(x => x.Name).ToList() ?? [];
        _departments = (await deptsTask)?.Where(x => x.IsActive).OrderBy(x => x.Name).ToList() ?? [];
        _positions = (await posTask)?.Where(x => x.IsActive).OrderBy(x => x.Name).ToList() ?? [];
        _schedules = (await schedsTask)?.Where(x => x.IsActive).OrderBy(x => x.Name).ToList() ?? [];
        _banks = (await banksTask)?.Where(x => x.IsActive).OrderBy(x => x.ShortName).ToList() ?? [];

        if (!_isNew)
        {
            var emp = await client.GetFromJsonAsync<EmployeeFormModel>($"/api/hr/employees/{EmployeeId}");
            if (emp is not null)
            {
                _form = emp;
            }
        }
        else
        {
            if (_companies.Count > 0) _form.CompanyId = _companies[0].CompanyId;
            if (_branches.Count > 0) _form.BranchId = _branches[0].BranchId;
            _form.HireDate = DateTime.Today;
            _form.Status = "active";
            _form.IsActive = true;
            _form.PrintReceipt = true;
            _form.ContractType = "indefinite";
            _form.EmployeeType = "base";
            _form.PayrollPeriodType = "semanal";
            _form.TaxRegime = "sueldos_salarios";
            _form.CotizationBase = "fixed";
            _form.PaymentForm = "tarjeta";
            _form.SalaryZone = "A";
        }

        _loading = false;
    }

    private async Task OnCompanyChanged()
    {
        if (_form.CompanyId == Guid.Empty) return;
        var client = HttpClientFactory.CreateClient("Nanchesoft.Api");
        _branches = (await client.GetFromJsonAsync<List<BranchItem>>($"/api/organization/branches?companyId={_form.CompanyId}"))
            ?.Where(x => x.IsActive).OrderBy(x => x.Name).ToList() ?? [];
        _departments = (await client.GetFromJsonAsync<List<DeptItem>>("/api/hr/departments"))
            ?.Where(x => x.IsActive && (x.CompanyId == _form.CompanyId || x.CompanyId == null)).OrderBy(x => x.Name).ToList() ?? [];
        _positions = (await client.GetFromJsonAsync<List<PosItem>>("/api/hr/positions"))
            ?.Where(x => x.IsActive && (x.CompanyId == _form.CompanyId || x.CompanyId == null)).OrderBy(x => x.Name).ToList() ?? [];
        _form.BranchId = _branches.FirstOrDefault()?.BranchId;
        _form.DepartmentId = null;
        _form.PositionId = null;
    }

    private async Task SaveAsync()
    {
        _saving = true;
        _error = string.Empty;
        _success = string.Empty;

        try
        {
            var client = HttpClientFactory.CreateClient("Nanchesoft.Api");
            HttpResponseMessage response;
            if (_isNew)
                response = await client.PostAsJsonAsync("/api/hr/employees", _form);
            else
                response = await client.PutAsJsonAsync($"/api/hr/employees/{EmployeeId}", _form);

            if (response.IsSuccessStatusCode)
            {
                _success = _isNew ? "Colaborador creado correctamente." : "Cambios guardados correctamente.";
                if (_isNew)
                {
                    var result = await response.Content.ReadFromJsonAsync<IdResult>();
                    if (result?.Id.HasValue == true)
                        Nav.NavigateTo($"/human-resources/empleado/{result.Id.Value}");
                }
            }
            else
            {
                var body = await response.Content.ReadAsStringAsync();
                _error = $"Error: {body}";
            }
        }
        catch (Exception ex)
        {
            _error = ex.Message;
        }
        finally
        {
            _saving = false;
        }
    }

    // ── Quick-add Department ──
    private void OpenDeptModal() { _newDept = new(); _deptError = string.Empty; _showDeptModal = true; }
    private async Task SaveDeptAsync()
    {
        _savingModal = true; _deptError = string.Empty;
        try
        {
            var client = HttpClientFactory.CreateClient("Nanchesoft.Api");
            var payload = new { CompanyId = _form.CompanyId == Guid.Empty ? (Guid?)null : _form.CompanyId, _newDept.Code, _newDept.Name, IsActive = true };
            var r = await client.PostAsJsonAsync("/api/hr/departments", payload);
            if (!r.IsSuccessStatusCode) { _deptError = await r.Content.ReadAsStringAsync(); return; }
            var created = await r.Content.ReadFromJsonAsync<IdResult>();
            _departments = (await client.GetFromJsonAsync<List<DeptItem>>("/api/hr/departments"))
                ?.Where(x => x.IsActive).OrderBy(x => x.Name).ToList() ?? [];
            _form.DepartmentId = created?.Id;
            _showDeptModal = false;
        }
        finally { _savingModal = false; }
    }

    // ── Quick-add Position ──
    private void OpenPosModal() { _newPos = new(); _posError = string.Empty; _showPosModal = true; }
    private async Task SavePosAsync()
    {
        _savingModal = true; _posError = string.Empty;
        try
        {
            var client = HttpClientFactory.CreateClient("Nanchesoft.Api");
            var payload = new { CompanyId = _form.CompanyId == Guid.Empty ? (Guid?)null : _form.CompanyId, _newPos.DepartmentId, _newPos.Code, _newPos.Name, IsActive = true };
            var r = await client.PostAsJsonAsync("/api/hr/positions", payload);
            if (!r.IsSuccessStatusCode) { _posError = await r.Content.ReadAsStringAsync(); return; }
            var created = await r.Content.ReadFromJsonAsync<IdResult>();
            _positions = (await client.GetFromJsonAsync<List<PosItem>>("/api/hr/positions"))
                ?.Where(x => x.IsActive).OrderBy(x => x.Name).ToList() ?? [];
            _form.PositionId = created?.Id;
            _showPosModal = false;
        }
        finally { _savingModal = false; }
    }

    // ── Quick-add WorkSchedule ──
    private void OpenSchedModal() { _newSched = new(); _schedError = string.Empty; _showSchedModal = true; }
    private async Task SaveSchedAsync()
    {
        _savingModal = true; _schedError = string.Empty;
        try
        {
            var client = HttpClientFactory.CreateClient("Nanchesoft.Api");
            var payload = new { CompanyId = _form.CompanyId == Guid.Empty ? (Guid?)null : _form.CompanyId, _newSched.Code, _newSched.Name, _newSched.WeeklyHours, Monday = true, Tuesday = true, Wednesday = true, Thursday = true, Friday = true, Saturday = false, Sunday = false, IsFlexible = false, IsActive = true };
            var r = await client.PostAsJsonAsync("/api/hr/work-schedules", payload);
            if (!r.IsSuccessStatusCode) { _schedError = await r.Content.ReadAsStringAsync(); return; }
            var created = await r.Content.ReadFromJsonAsync<IdResult>();
            _schedules = (await client.GetFromJsonAsync<List<SchedItem>>("/api/hr/work-schedules"))
                ?.Where(x => x.IsActive).OrderBy(x => x.Name).ToList() ?? [];
            _form.WorkScheduleId = created?.Id;
            _showSchedModal = false;
        }
        finally { _savingModal = false; }
    }

    // ── Quick-add Bank ──
    private void OpenBankModal() { _newBank = new(); _bankError = string.Empty; _showBankModal = true; }
    private async Task SaveBankAsync()
    {
        _savingModal = true; _bankError = string.Empty;
        try
        {
            var client = HttpClientFactory.CreateClient("Nanchesoft.Api");
            var payload = new { _newBank.Code, _newBank.ShortName, _newBank.Name, SatCode = _newBank.SatCode, IsActive = true };
            var r = await client.PostAsJsonAsync("/api/hr/banks", payload);
            if (!r.IsSuccessStatusCode) { _bankError = await r.Content.ReadAsStringAsync(); return; }
            _banks = (await client.GetFromJsonAsync<List<BankItem>>("/api/hr/banks"))
                ?.Where(x => x.IsActive).OrderBy(x => x.ShortName).ToList() ?? [];
            _form.BankCode = _newBank.Code;
            _showBankModal = false;
        }
        finally { _savingModal = false; }
    }

    // ─────────── Inner types ───────────
    private sealed class EmployeeFormModel
    {
        public Guid? CompanyId { get; set; }
        public Guid? BranchId { get; set; }
        public Guid? DepartmentId { get; set; }
        public Guid? PositionId { get; set; }
        public Guid? WorkScheduleId { get; set; }
        public string Code { get; set; } = string.Empty;
        public string EmployeeNumber { get; set; } = string.Empty;
        public string? ClockKey { get; set; }
        public string? NoiKey { get; set; }
        public string FirstName { get; set; } = string.Empty;
        public string LastName { get; set; } = string.Empty;
        public string? SecondLastName { get; set; }
        public string? MiddleName { get; set; }
        public string? Email { get; set; }
        public string? Phone { get; set; }
        public string? EmergencyPhone { get; set; }
        public string? TaxId { get; set; }
        public string? NationalId { get; set; }
        public string? Curp { get; set; }
        public string? Nss { get; set; }
        public string? ImssRegId { get; set; }
        public string? Gender { get; set; }
        public string? BloodType { get; set; }
        public string? MaritalStatus { get; set; }
        public string? PlaceOfBirth { get; set; }
        public string? Nationality { get; set; }
        public string? FatherName { get; set; }
        public string? MotherName { get; set; }
        public string? AddressStreet { get; set; }
        public string? AddressColony { get; set; }
        public string? AddressCity { get; set; }
        public string? AddressState { get; set; }
        public string? AddressZipCode { get; set; }
        public decimal DailySalary { get; set; }
        public decimal IntegratedDailySalary { get; set; }
        public decimal SbcFija { get; set; }
        public string Status { get; set; } = "active";
        public DateTime? HireDate { get; set; }
        public DateTime? BirthDate { get; set; }
        public DateTime? TerminationDate { get; set; }
        public string? TerminationReason { get; set; }
        public DateTime? ReentryDate { get; set; }
        public bool IsImssRegistered { get; set; }
        public DateTime? ImssRegistrationDate { get; set; }
        public DateTime? ImssTerminationDate { get; set; }
        public string? Umf { get; set; }
        public string ContractType { get; set; } = "indefinite";
        public string CotizationBase { get; set; } = "fixed";
        public string TaxRegime { get; set; } = "sueldos_salarios";
        public string EmployeeType { get; set; } = "base";
        public string SalaryZone { get; set; } = "A";
        public string PayrollPeriodType { get; set; } = "semanal";
        public string? Afore { get; set; }
        public string? Fonacot { get; set; }
        public string? Infonavit { get; set; }
        public string PaymentForm { get; set; } = "tarjeta";
        public string? BankCode { get; set; }
        public string? BankAccount { get; set; }
        public string? Clabe { get; set; }
        public string? BankBranch { get; set; }
        public string? ImmediateSupervisor { get; set; }
        public string? Category { get; set; }
        public string? Notes { get; set; }
        public bool PrintReceipt { get; set; } = true;
        public bool IsActive { get; set; } = true;

        public string FullName() => string.Join(" ", new[] { FirstName, LastName, SecondLastName }
            .Where(x => !string.IsNullOrWhiteSpace(x)));
    }

    private sealed record CompanyItem(Guid CompanyId, string Name, bool IsActive);
    private sealed record BranchItem(Guid BranchId, string Name, bool IsActive);
    private sealed record DeptItem(Guid DepartmentId, Guid? CompanyId, string Name, bool IsActive);
    private sealed record PosItem(Guid PositionId, Guid? CompanyId, string Name, bool IsActive);
    private sealed record SchedItem(Guid WorkScheduleId, string Name, bool IsActive);
    private sealed record BankItem(Guid BankId, string Code, string ShortName, string Name, bool IsActive);
    private sealed record IdResult(Guid? Id);

    private sealed class NewDeptForm { public string Code { get; set; } = string.Empty; public string Name { get; set; } = string.Empty; }
    private sealed class NewPosForm { public string Code { get; set; } = string.Empty; public string Name { get; set; } = string.Empty; public Guid? DepartmentId { get; set; } }
    private sealed class NewSchedForm { public string Code { get; set; } = string.Empty; public string Name { get; set; } = string.Empty; public decimal WeeklyHours { get; set; } = 40; }
    private sealed class NewBankForm { public string Code { get; set; } = string.Empty; public string ShortName { get; set; } = string.Empty; public string Name { get; set; } = string.Empty; public string? SatCode { get; set; } }
}
